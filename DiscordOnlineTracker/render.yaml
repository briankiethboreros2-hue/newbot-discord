services:
  - type: worker
    name: imperial-bot
    env: python
    plan: free
    region: frankfurt  # Different from default
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    restartPolicy: always
